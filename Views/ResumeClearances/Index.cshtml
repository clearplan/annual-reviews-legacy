@model IEnumerable<CP.AnnualReviews.Models.TblResumeClearance>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ResumeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clearance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IssuedBy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvestigationType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProgramAccess)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdditionalInvestigations)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResumeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Clearance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IssuedBy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvestigationType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramAccess)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AdditionalInvestigations)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
